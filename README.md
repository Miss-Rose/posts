README ------------------------------------------------------------------------

In order to starts you need:

npm install
npm install -g json-server
cd/ JSON-server 
json-server --watch db.json --port 3001
cd/ app
npm start
